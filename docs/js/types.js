/** @typedef {{id:string, headers:Object<string,string>, movetext:string, raw:string}} Game */
/** @typedef {{id:string, ts:string, epochStart:number, epochEnd:number, color:'W'|'B',
 *   res:'W'|'L'|'D', oppD:number, rating:number, tc:string, eco:string,
 *   term:'mate'|'timeout'|'resign'|'abandon'|'draw'|'other',
 *   gapS:number, sess:number, lstreak:number,
 *   clk12pct:number, mvU30s:number|null, finclkS:number, moves:number}} GameRow */

export {};
